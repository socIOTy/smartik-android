<resources>

    <!-- Base application theme. -->

    <style name="smartikTheme" parent="Theme.AppCompat.Light">
        <item name="colorAccent">@color/colorAccent</item>
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
    </style>

    <declare-styleable name="CheckableButton">
        <attr name="is_checked" format="boolean" />

        <attr name="personality">
            <enum name="radio" value="0" />
            <enum name="check" value="1" />
        </attr>
    </declare-styleable>

    <style name="smartikTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="smartikTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="smartikTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
